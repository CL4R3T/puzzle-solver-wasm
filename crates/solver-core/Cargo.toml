[package]
name = "solver-core"
version = "0.1.0"
edition = "2021"
description = "Pure Rust puzzle solver engine — constraint propagation + backtracking"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
instant = { version = "0.1", features = ["wasm-bindgen"] }
